<h2>All users</h2>
<ul>
  <% @users.each do |user| %>
  <% next if user == current_user %>
    <li>
      <%= user.email %> - <%= link_to 'Add Friend', create_friendship_path(user.id), method: 'POST' %>
    </li>
  <% end %>
</ul>
<%= link_to 'Check your friends', friendships_path %>
<%= link_to  "Home", root_path %>